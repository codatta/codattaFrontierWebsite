import { useParams } from 'react-router-dom'

export default function AirdropModelComparison({ templateId }: { templateId: string }) {
  const { taskId } = useParams()

  return <div>AirdropModelComparison</div>
}
